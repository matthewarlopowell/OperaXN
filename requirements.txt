# requirements.txt
# Python >= 3.9

# core pipeline
numpy>=1.20.0
pandas>=2.0.0
h5py>=3.0.0
fabio>=0.14.0

# GUI / analysis
matplotlib>=3.4.0
openpyxl>=3.0.0
psutil>=5.8.0
imageio>=2.9.0

# dev only (optional): lxml for NXDL schema validation, pynxtools for
# validating generated files against the definitions in definitions/
# lxml>=4.9
# pynxtools
